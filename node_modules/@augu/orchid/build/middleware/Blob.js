"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
/**
 * Enables the ability to pass in Blob-data structures
 */
exports.default = () => ({
    intertwine() {
        const logger = this.middleware.get('logger');
        if (logger)
            logger.info('Enabled Blob middleware, now you have access to pass in Blobs in Request#body and access to Response#blob');
        this.middleware.add('blob', true);
    },
    cycleType: _1.CycleType.None,
    name: 'blob'
});
