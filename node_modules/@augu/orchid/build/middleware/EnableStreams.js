"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
/**
 * Enables streams to pipe data easier
 * @returns {Middleware} A middleware function to use with `HttpClient#use`
 */
const streams = () => ({
    name: 'streams',
    cycleType: _1.CycleType.None,
    intertwine() {
        this.middleware.add('streams', true);
        const logger = this.middleware.get('logger');
        if (logger)
            logger.info('Enabled streams, now you have access to Response#stream');
    }
});
exports.default = streams;
