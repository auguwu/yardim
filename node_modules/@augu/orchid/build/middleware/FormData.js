"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
/**
 * Enables the ability to pass in Forms
 * @returns {Middleware}
 */
exports.default = () => ({
    intertwine() {
        try {
            require('form-data');
        }
        catch {
            const logger = this.middleware.get('logger');
            if (logger)
                logger.warn('Unable to find package `form-data`, skipping...');
        }
        const logger = this.middleware.get('logger');
        if (logger)
            logger.info('Enabled Forms middleware, now you have access to pass in Form Data into Request#body');
        this.middleware.add('form', true);
    },
    cycleType: _1.CycleType.None,
    name: 'form'
});
