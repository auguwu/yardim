"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Checks if a property exists in an object, returns a default value if none was provided
 * @param prop The property to find
 * @param defaultValue The default value if not found
 * @param options The options itself
 */
// eslint-disable-next-line
function getOption(prop, defaultValue, options) {
    if (options === undefined)
        return defaultValue;
    else if (options.hasOwnProperty(prop))
        return options[prop];
    else
        return defaultValue;
}
exports.default = getOption;
